# PII 自定义词汇黑名单
#
# 用途：凡出现在此列表中的词汇，无论上下文，均直接被替换为 [内部机密]。
#       适合填写内部代号、未公开项目名、敏感术语等固定字符串。
#
# 使用方式：
#   - 每条词汇独占一行，以 "- " 开头
#   - 支持中文、英文、混合词汇
#   - 修改后重启服务生效（无需修改代码）
#
# 环境变量覆盖路径：PII_DENY_LIST_PATH=/path/to/your/deny_list.yaml

terms:
  # ── 内部项目代号 ──────────────────────────────────────────────
  - 阿波罗计划
  - X09引擎
  - 海神系统

  # ── 未公开财务数据 ─────────────────────────────────────────────
  - Q3未发布财报

  # ── 在此添加更多词汇 ───────────────────────────────────────────
  # - 新项目代号
  # - 内部系统名称
